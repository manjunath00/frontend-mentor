{
  "name": "frontend-mentor",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "sass": "node-sass -w sass -o public/css",
    "parcel": "parcel index.html --open",
    "watch": "yarn run sass & yarn run parcel",
    "build": "parcel build index.html -o dist/01-tip.html"
  },
  "dependencies": {
    "node-sass": "^6.0.1"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.5",
    "sass": "^1.43.4"
  }
}
